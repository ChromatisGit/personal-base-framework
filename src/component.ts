// Component-authoring API — only for building central custom app components
// (e.g., a Roadmap, canvas, or other domain-specific UI that can't use framework components directly)
// Do NOT import this in normal route/page code — use @platform/framework instead.
export { cn } from "./ui/primitives/cn"
export { useBreakpoint } from "./ui/lib/useBreakpoint"
export { useReducedMotion } from "./ui/lib/useReducedMotion"
